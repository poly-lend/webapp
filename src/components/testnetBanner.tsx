export default function TestnetBanner() {
  return (
    <div
      role="alert"
      className="w-full bg-red-600 text-white text-sm font-semibold text-center px-4 py-2 tracking-wide"
    >
      TESTNET · transactions have no real value
    </div>
  )
}
